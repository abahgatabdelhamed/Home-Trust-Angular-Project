import { ArabicMonthPipe } from './arabic-month.pipe';

describe('ArabicMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new ArabicMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
